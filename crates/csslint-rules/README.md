# csslint-rules

Rule registry and deterministic rule execution.

Responsibilities:

- Register core v1 rules by stable rule ID.
- Execute enabled rules in deterministic order.
- Emit diagnostics using shared core contracts.
