# csslint-config

Configuration loading and validation.

Responsibilities:

- Discover and parse `.csslint` JSON configuration.
- Expand presets into explicit rule severities.
- Validate known rule IDs and accepted severity values.
