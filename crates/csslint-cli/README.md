# csslint-cli

Command line entrypoint and orchestration layer.

Responsibilities:

- Parse CLI args and select reporter mode.
- Drive extraction, parsing, semantic analysis, rules, and fixes.
- Resolve process exit codes for lint and runtime outcomes.
