# csslint-core

Shared core contracts used by all CSSLint crates.

Responsibilities:

- Fundamental IDs, spans, diagnostics, severities, and scope markers.
- Shared data contracts that cross crate boundaries.
- No parser or CLI-specific behavior.
