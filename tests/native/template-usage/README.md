# Template Usage Fixtures

This directory scaffolds provider fixtures described in `docs/template-usage-fixtures-v1.md`.

Each case directory includes:

- `input.vue` or `input.svelte`
- `expected.json`

These are starter fixtures and can be expanded as provider behavior is implemented.
