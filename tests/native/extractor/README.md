# Extractor Fixtures

Fixture corpus for `csslint-extractor` behavior and source mapping.

Each fixture directory contains:

- `input.css`, `input.vue`, or `input.svelte`
- `expected.json`

`expected.json` defines extracted style block order/metadata and expected diagnostics.
