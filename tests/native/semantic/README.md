# Semantic Fixtures

Snapshot fixtures for parser + semantic integration.

Each case includes:

- `input.css` / `input.vue` / `input.svelte`
- `expected.json`

Expected snapshots cover normalized selectors, selector-part scope annotations,
declaration properties, and semantic index summaries.
